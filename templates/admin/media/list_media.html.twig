{% extends 'admin/baseAdmin.html.twig' %}

{% block body %}
    <div class="container">
        {% if medias != [] %}
            {% for media in medias %}
                <li>
                    <div>
                        <span>{{ media.name }}</span>
                        <img width="150" height="200" src="{{ asset(media.path) }}" alt="{{ media.name }}">
                    </div>
                </li>
            {% endfor %}
        {% else %}
            <li>Aucun média du type "{{ mediaType }}" n'a été trouvé.</li>
        {% endif %}
    </div>
{% endblock %}