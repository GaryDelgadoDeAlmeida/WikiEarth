{% extends 'admin/baseAdmin.html.twig' %}

{% block body %}
    <div class="livingThing-form-section">
        <a class="btn btn-primary" href="{{ path('adminLivingThing') }}"><i class="fas fa-arrow-left"></i> Retour</a>
        <div>&nbsp;</div>

        {% if response != [] %}
            <div class="alert alert-{{ response.class }} txt-center">
                <span>{{ response.message }}</span>
            </div>
        {% endif %}

        <h2>Living Thing</h2>

        {{ form_start(formLivingThing, {"method": "POST"}) }}
            <div class="row">
                <div class="form-group col-md-12 col-lg-12 wow fadeInUp animated">
                    {{ form_label(formLivingThing.imgPath) }}
                    {{ form_widget(formLivingThing.imgPath) }}
                </div>

                <div class="form-group col-md-12 col-lg-12 wow fadeInUp animated">
                    {{ form_label(formLivingThing.commonName) }}
                    {{ form_widget(formLivingThing.commonName) }}
                </div>

                <div class="form-group col-md-12 col-lg-12 wow fadeInUp animated">
                    {{ form_label(formLivingThing.name) }}
                    {{ form_widget(formLivingThing.name) }}
                </div>

                {# Domain #}
                <h2>Domain</h2>
                <div class="form-group col-md-12 col-lg-12 wow fadeInUp animated">
                    {{ form_label(formLivingThing.domain) }}
                    {{ form_widget(formLivingThing.domain) }}
                </div>

                <div class="form-group col-md-12 col-lg-12 wow fadeInUp animated">
                    {{ form_label(formLivingThing.subDomain) }}
                    {{ form_widget(formLivingThing.subDomain) }}
                </div>

                {# Kingdom #}
                <h2>Kingdom</h2>
                <div class="form-group col-md-12 col-lg-12 wow fadeInUp animated">
                    {{ form_label(formLivingThing.superKingdom) }}
                    {{ form_widget(formLivingThing.superKingdom) }}
                </div>

                <div class="form-group col-md-12 col-lg-12 wow fadeInUp animated">
                    {{ form_label(formLivingThing.kingdom) }}
                    {{ form_widget(formLivingThing.kingdom) }}
                </div>

                <div class="form-group col-md-12 col-lg-12 wow fadeInUp animated">
                    {{ form_label(formLivingThing.subKingdom) }}
                    {{ form_widget(formLivingThing.subKingdom) }}
                </div>

                <div class="form-group col-md-12 col-lg-12 wow fadeInUp animated">
                    {{ form_label(formLivingThing.infraKingdom) }}
                    {{ form_widget(formLivingThing.infraKingdom) }}
                </div>

                {# Phylum (animalia) #}
                <h2>Phylum</h2>
                <div class="form-group col-md-12 col-lg-12 wow fadeInUp animated">
                    {{ form_label(formLivingThing.superBranch) }}
                    {{ form_widget(formLivingThing.superBranch) }}
                </div>

                <div class="form-group col-md-12 col-lg-12 wow fadeInUp animated">
                    {{ form_label(formLivingThing.branch) }}
                    {{ form_widget(formLivingThing.branch) }}
                </div>

                <div class="form-group col-md-12 col-lg-12 wow fadeInUp animated">
                    {{ form_label(formLivingThing.subBranch) }}
                    {{ form_widget(formLivingThing.subBranch) }}
                </div>

                <div class="form-group col-md-12 col-lg-12 wow fadeInUp animated">
                    {{ form_label(formLivingThing.infraBranch) }}
                    {{ form_widget(formLivingThing.infraBranch) }}
                </div>

                {# Division (plantae, fungis) #}
                <h2>Division</h2>
                <div class="form-group col-md-12 col-lg-12 wow fadeInUp animated">
                    {{ form_label(formLivingThing.division) }}
                    {{ form_widget(formLivingThing.division) }}
                </div>

                {# Class #}
                <h2>Class</h2>
                <div class="form-group col-md-12 col-lg-12 wow fadeInUp animated">
                    {{ form_label(formLivingThing.superClass) }}
                    {{ form_widget(formLivingThing.superClass) }}
                </div>

                <div class="form-group col-md-12 col-lg-12 wow fadeInUp animated">
                    {{ form_label(formLivingThing.class) }}
                    {{ form_widget(formLivingThing.class) }}
                </div>

                <div class="form-group col-md-12 col-lg-12 wow fadeInUp animated">
                    {{ form_label(formLivingThing.subClass) }}
                    {{ form_widget(formLivingThing.subClass) }}
                </div>

                <div class="form-group col-md-12 col-lg-12 wow fadeInUp animated">
                    {{ form_label(formLivingThing.infraClass) }}
                    {{ form_widget(formLivingThing.infraClass) }}
                </div>

                {# Order #}
                <h2>Order</h2>
                <div class="form-group col-md-12 col-lg-12 wow fadeInUp animated">
                    {{ form_label(formLivingThing.superOrder) }}
                    {{ form_widget(formLivingThing.superOrder) }}
                </div>

                <div class="form-group col-md-12 col-lg-12 wow fadeInUp animated">
                    {{ form_label(formLivingThing.normalOrder) }}
                    {{ form_widget(formLivingThing.normalOrder) }}
                </div>

                <div class="form-group col-md-12 col-lg-12 wow fadeInUp animated">
                    {{ form_label(formLivingThing.subOrder) }}
                    {{ form_widget(formLivingThing.subOrder) }}
                </div>

                <div class="form-group col-md-12 col-lg-12 wow fadeInUp animated">
                    {{ form_label(formLivingThing.infraOrder) }}
                    {{ form_widget(formLivingThing.infraOrder) }}
                </div>

                <div class="form-group col-md-12 col-lg-12 wow fadeInUp animated">
                    {{ form_label(formLivingThing.microOrder) }}
                    {{ form_widget(formLivingThing.microOrder) }}
                </div>

                {# Family #}
                <h2>Family</h2>
                <div class="form-group col-md-12 col-lg-12 wow fadeInUp animated">
                    {{ form_label(formLivingThing.superFamily) }}
                    {{ form_widget(formLivingThing.superFamily) }}
                </div>

                <div class="form-group col-md-12 col-lg-12 wow fadeInUp animated">
                    {{ form_label(formLivingThing.family) }}
                    {{ form_widget(formLivingThing.family) }}
                </div>

                <div class="form-group col-md-12 col-lg-12 wow fadeInUp animated">
                    {{ form_label(formLivingThing.subFamily) }}
                    {{ form_widget(formLivingThing.subFamily) }}
                </div>

                {# Tribe #}
                <h2>Tribe</h2>
                <div class="form-group col-md-12 col-lg-12 wow fadeInUp animated">
                    {{ form_label(formLivingThing.tribe) }}
                    {{ form_widget(formLivingThing.tribe) }}
                </div>

                <div class="form-group col-md-12 col-lg-12 wow fadeInUp animated">
                    {{ form_label(formLivingThing.subTribe) }}
                    {{ form_widget(formLivingThing.subTribe) }}
                </div>

                {# Genus #}
                <h2>Genus</h2>
                <div class="form-group col-md-12 col-lg-12 wow fadeInUp animated">
                    {{ form_label(formLivingThing.genus) }}
                    {{ form_widget(formLivingThing.genus) }}
                </div>

                <div class="form-group col-md-12 col-lg-12 wow fadeInUp animated">
                    {{ form_label(formLivingThing.subGenus) }}
                    {{ form_widget(formLivingThing.subGenus) }}
                </div>

                {# Species #}
                <h2>Species</h2>
                <div class="form-group col-md-12 col-lg-12 wow fadeInUp animated">
                    {{ form_label(formLivingThing.species) }}
                    {{ form_widget(formLivingThing.species) }}
                </div>

                <div class="form-group col-md-12 col-lg-12 wow fadeInUp animated">
                    {{ form_label(formLivingThing.subSpecies) }}
                    {{ form_widget(formLivingThing.subSpecies) }}
                </div>

                <div class="form-group col-md-12 col-lg-12 wow fadeInUp animated">
                    {{ form_label(formLivingThing.countries) }}
                    {{ form_widget(formLivingThing.countries) }}
                </div>

                <div class="form-group col-md-12 col-lg-12 wow fadeInUp animated">
                    <button class="btn btn-custom-blue" type="submit">Submit</button>
                </div>
            </div>
        {{ form_end(formLivingThing, {"method": "POST"}) }}
    </div>
{% endblock %}